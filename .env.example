PORT=3001
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=pineda_dash

# Clave para firmar los tokens de sesion (JWT). En produccion (Railway) debe
# configurarse con un valor propio y secreto, distinto al de este ejemplo.
JWT_SECRET=cambia-esto-por-un-valor-largo-y-aleatorio
